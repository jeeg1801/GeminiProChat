# 捡漏自动化引擎 · 设计图（音响配件 A 腿）
**一条流水线 = 捡漏监控 + 自动视频脚本 + 品牌/需求知识库筛选**
版本 v1 · 2026-06-14

> ⚠️ 我无法实时打开 Yahoo!オークション / Mercari / Hard-Off，本文里所有日元/美元数字
> 都是**结构性示意**，需你用真实截图校准（见 §6 校准流程）。

---

## 1. 一句话理念（你的构想）
**引擎发现一件捡漏好货 → 自动判断值不值得抢（靠知识库打分）→ 值得就预警 +
同时自动吐出这件的视频拍摄脚本。** 捡漏和内容，一次产出。

---

## 2. 流水线全景（数据怎么流）

```
[1] 定时触发 (每 15~30 分钟)
      │
[2] 读「监控词表」(Google Sheet: Watchlist)
      │  ← 品牌×配件×ジャンク 组合关键词
      ▼
[3] 抓取各平台搜索结果 (Yahoo / Hard-Off / Mercari)
      │  → 标题 / 价格 / 链接 / 图 / 成色词
      ▼
[4] 解析 + 匹配「需求知识库」(Google Sheet: KB)
      │  → 得到：欧美售价区间 / 需求等级 / 运输等级 / 目标进货价
      ▼
[5] 打分引擎 (Function 节点)
      │  → 利润、毛利%、需求权重、风险扣分 → 捡漏分
      ▼
[6] 去重 (对照 Seen 表，避免重复预警)
      │
[7] IF 捡漏分 ≥ 阈值 ?
      ├─ 否 → 丢弃
      └─ 是 ▼
[8] 写入「捡漏看板」(Google Sheet: Deals)
      │
[9] LLM 节点 → 自动生成「该件视频脚本」
      │
[10] 推送预警 (Telegram / LINE / 邮件) 含：图 + 价 + 分 + 脚本草稿
```

> 复用关系：这是把简报里的 `n8n_trend_engine.json` 改造成"捡漏监控"；
> 抢到货后的发货/CRM 走原 `n8n_fulfillment_engine.json`，不重复造轮子。

---

## 3. 评分引擎（系统的大脑 / 核心 IP）

每条抓到的商品，按下面算：

```
# 输入
buy_price_jpy   = 商品当前价（日元）
resale_usd_mid  = 知识库里该品类欧美中位售价（美元）
fx_usdjpy       = 美元兑日元汇率（参数，例 155）
ship_cost_jpy   = 该运输等级预估国际运费
fees_jpy        = 代拍/平台/支付手续费预估

# 计算
revenue_jpy = resale_usd_mid * fx_usdjpy
cost_jpy    = buy_price_jpy + ship_cost_jpy + fees_jpy
profit_jpy  = revenue_jpy - cost_jpy
margin_pct  = profit_jpy / cost_jpy

demand_weight = { S:1.0, A:0.7, B:0.4 }[需求等级]
risk_penalty  = { light:0, med:10, heavy:30 }[运输等级] + (含变压器/电压问题 ? 15 : 0)

deal_score = margin_pct * 100 * demand_weight - risk_penalty
```

**预警门槛（可调）：**
```
ALERT  当  margin_pct >= 0.6   且  profit_jpy >= 8000   且  需求等级 ∈ {S, A}
HOT🔥  当  deal_score >= 80     （置顶 + 手机强提醒）
```

> 这三个阈值（0.6 / 8000 / 80）开局保守，跑两周用真实成交数据回调。

### 成色关键词加权（标题里出现就调分）
| 日文成色词 | 含义 | 对分数 |
|---|---|---|
| ジャンク / 不動 / 故障 | 坏/不通电 | 进货便宜 → 加分；但若你卖整机要扣 |
| 動作未確認 | 未测试 | 压价空间 → 小加分 |
| 美品 / 極美品 | 品相极好 | 卖相好 → 加分（适合 B 腿翻新成品）|
| 部品取り / まとめ | 拆件/打包 | 拆件采集首选 → 加分 |
| 訳あり | 有瑕疵 | 视情况 |

---

## 4. 视频脚本自动生成（内容跟着货走）

打分过关的商品，喂给 LLM 节点，用下面这段 **Prompt 模板**自动出脚本：

```
你是一位电影导演 + 复古音响修复师。根据这件刚捡漏到的器材，写一条 60~90 秒
短视频拍摄脚本，电影质感、强叙事、面向欧美黑胶/复古音响爱好者（英文成片）。

器材信息：
- 品牌/型号：{{brand}} {{model}}
- 品类：{{category}}
- 成色：{{condition}}
- 拆解可取配件：{{harvest_parts}}
- 卖点关键词：{{selling_points}}

输出结构：
1. HOOK（前3秒钩子，一句让人停下）
2. 镜头清单（逐镜：画面 + 旁白/字幕），突出 木纹/旋钮/表头/暖光 等电影元素
3. 拆解高光（before → after / 关键配件特写）
4. 价值点字幕（稀缺性 / 为什么修复者需要它）
5. CTA（引导到独立站 / 配件链接 / 关注）
6. 3 个备选标题 + 10 个英文长尾关键词（给 SEO 用）
```

→ 输出直接写进 Deals 表对应行 + 推送到你手机，你拿着就能拍。

---

## 5. 各平台抓取现实（坑要提前知道）

| 平台 | 抓取难度 | 方案 | 备注 |
|---|---|---|---|
| **Yahoo!オークション** | 中 | HTTP 抓搜索页 + HTML 解析；部分分类有 RSS | 最主力、最可行 |
| **Hard-Off NetMall** | 中 | HTTP 抓 netmall 搜索结果 + 解析 | ジャンク 拆件机金矿 |
| **Mercari** | **高** | 页面 JS 渲染、有反爬、官方无公开 API | 开局可先**手动刷 + 半自动**，不要卡在这 |
| **秋叶原元器件店(NOS)** | 线下 | 不在自动化内 → 走 §"侦察清单"实地建供货 | S1 金矿靠人 |

> 务实建议：**先把 Yahoo + Hard-Off 两条跑通**（覆盖 80% 捡漏机会），Mercari 二期再说。
> 抓取请尊重各站条款、控制频率（每 15~30 分钟、加随机延迟），别把 IP 干封了。

---

## 6. 校准流程（让示意数字变成真数字）
1. 你按知识库里的词，去 Yahoo/Hard-Off 各抓 10~20 条真实成交截图给我
2. 我帮你回填：真实进货价区间、真实欧美售价（eBay sold 截图）、真实运费
3. 更新 KB 的 `resale_usd` 和 `target_max_buy_jpy`
4. 跑两周 → 用实际抢到/卖出数据，回调三个阈值

---

## 7. 落地步骤（你/我分工）
- [ ] **你**：建 3 个 Google Sheet 标签页：`Watchlist` / `KB` / `Deals` / `Seen`
- [ ] **你**：把 `audio_demand_kb.csv` 导进 KB 表
- [ ] **你**：n8n 里配好 Google Sheets、LLM、Telegram/LINE 三个凭据
- [ ] **我**：`n8n_audio_picker_engine.json` 骨架已给，按你的凭据/词表微调
- [ ] **一起**：先跑 Yahoo 一条链路 → 验证打分 → 再加 Hard-Off → 再接脚本生成

---

## 8. 监控词表（Watchlist 起步，可直接用）
组合公式：`品牌日文 + 配件日文 + (ジャンク/部品取り)`
- サンスイ ジャンク / サンスイ ノブ / サンスイ パネル
- ナカミチ ヘッド / ナカミチ ピンチローラー
- ラックスマン ジャンク / ラックスマン つまみ
- パイオニア SX ジャンク / マランツ ノブ
- DL-103 / オーディオテクニカ カートリッジ / ナガオカ 針
- 2SC3281 2SA1302 / 2SA627（NOS 晶体管，多在元器件店）
- ヤマハ NS-1000 ツイーター
- ムギ球 メーターランプ（暖光灯）
> 完整品牌×配件矩阵见 `audio_demand_kb.csv`。
